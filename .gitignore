# Brainstorming companion (local mockups + server state)
.superpowers/

# Flutter / Dart
.dart_tool/
.packages
.pub-cache/
.pub/
build/
.flutter-plugins
.flutter-plugins-dependencies
# NOTE: pubspec.lock is deliberately NOT ignored — Nouri is an application,
# and committing the lockfile keeps builds reproducible.

# Generated code (drift / riverpod / l10n)
*.g.dart
*.drift.dart
*.freezed.dart

# Android
android/.gradle/
android/.kotlin/
android/local.properties
android/key.properties
*.keystore
*.jks
android/app/release/
android/app/debug/

# IDE
.idea/
.vscode/
*.iml

# OS
Thumbs.db
desktop.ini
.DS_Store

# Secrets — never commit an API key
.env
secrets.json
flutter_*.log
